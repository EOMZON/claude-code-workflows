# Workflow name | 工作流名称
name: Claude Code Review
# 工作流名称：Claude 代码审查

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]
    # 触发类型：opened（创建）、synchronize（同步）、ready_for_review（准备审查）、reopened（重新打开）

jobs:
  claude-review:
    runs-on: ubuntu-latest
    # 运行环境：最新的 Ubuntu
    permissions:
      contents: read
      pull-requests: write
      issues: read
      id-token: write
      # 权限说明：读取仓库内容、撰写 PR 评论、读取 Issue、写入 ID 令牌
    
    steps:
      - name: Checkout repository
        # 步骤：检出仓库代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          # 仅拉取最近一次提交历史

      - name: Run Claude Code Review
        # 步骤：运行 Claude 代码审查
        id: claude-review
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          # Claude Code OAuth 令牌
          # or: claude-api-key: ${{ secrets.CLAUDE_API_KEY }}
          # When track_progress is enabled:
          # - Creates a tracking comment with progress checkboxes
          # - Includes all PR context (comments, attachments, images)
          # - Updates progress as the review proceeds
          # - Marks as completed when done
          # 选项说明（track_progress 启用时）：
          # - 创建包含进度复选框的跟踪评论
          # - 包含所有 PR 上下文（评论、附件、图片）
          # - 随审查过程更新进度
          # - 完成后标记为完成
          track_progress: true
          prompt: |
            REPO: ${{ github.repository }}
            仓库：${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}
            拉取请求编号：${{ github.event.pull_request.number }}

            Perform a comprehensive code review with the following focus areas:
            请根据以下重点执行一次全面的代码审查：

            1. **Code Quality**
               - Clean code principles and best practices
               - Proper error handling and edge cases
               - Code readability and maintainability
            1. **代码质量**
               - 遵循整洁代码原则与最佳实践
               - 正确处理错误与边界情况
               - 保证代码可读性与可维护性

            2. **Security**
               - Check for potential security vulnerabilities
               - Validate input sanitization
               - Review authentication/authorization logic
            2. **安全性**
               - 检查潜在的安全漏洞
               - 验证输入净化与校验
               - 审查认证/授权逻辑

            3. **Performance**
               - Identify potential performance bottlenecks
               - Review database queries for efficiency
               - Check for memory leaks or resource issues
            3. **性能**
               - 识别潜在性能瓶颈
               - 审查数据库查询效率
               - 检查内存泄漏或资源问题

            4. **Testing**
               - Verify adequate test coverage
               - Review test quality and edge cases
               - Check for missing test scenarios
            4. **测试**
               - 确认测试覆盖率是否充分
               - 审查测试质量与边界用例
               - 检查是否遗漏测试场景

            5. **Documentation**
               - Ensure code is properly documented
               - Verify README updates for new features
               - Check API documentation accuracy
            5. **文档**
               - 确认代码有完善的文档说明
               - 验证 README 是否更新新功能
               - 检查 API 文档是否准确

            Provide detailed feedback using inline comments for specific issues.
            Use top-level comments for general observations or praise.
            请使用行内评论反馈具体问题，并通过顶层评论给出总体观察或表扬。

            Use the repository's CLAUDE.md for guidance on style and conventions. Be constructive and helpful in your feedback.
            请参考仓库中的 CLAUDE.md 获取风格与约定指引，反馈需保持建设性并提供帮助。

            Use `gh pr comment` with your Bash tool to leave your review as a comment on the PR.
            使用 Bash 工具配合 `gh pr comment` 命令，将审查结果作为评论发布到 PR 中。

          # See https://github.com/anthropics/claude-code-action/blob/main/docs/usage.md
          # or https://docs.anthropic.com/en/docs/claude-code/sdk#command-line for available options
          claude_args: '--model claude-opus-4-1-20250805 --allowed-tools "mcp__github_inline_comment__create_inline_comment,Bash(gh issue view:*),Bash(gh search:*),Bash(gh issue list:*),Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*),Bash(gh pr list:*)"'
          # 模型与允许工具：claude-opus-4-1-20250805，允许的 GitHub CLI 相关操作
          
